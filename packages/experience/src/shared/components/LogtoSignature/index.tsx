import type { Theme } from '@logto/schemas';

type Props = {
  readonly className?: string;
  readonly theme: Theme;
};

const LogtoSignature = (_props: Props) => null;

export default LogtoSignature;
